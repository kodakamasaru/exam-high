# 未決定事項

## 🔴 優先度高（決めないと実装に入れない）

### 予約管理の具体的なAPI仕様
エンドポイント、データモデル、リクエスト/レスポンス形式、data-testid仕様を詳細に設計する必要がある。
- reservations + slots + resources
- ダブルブッキング防止がコア課題（並行処理）
- 時間枠クエリで性能差が出る（パフォーマンス）
- カレンダー的なUIでフロントに意味がある

### 配点の具体値
最新の評価軸に合わせた具体的なウェイト値。

**バックエンド（仮案）**
| 軸 | 配点 |
|---|---|
| アーキテクチャ品質（ADR宣言ベース + 循環依存） | 30 |
| 型安全性 | 25 |
| コード品質 | 15 |
| コード重複 | 10 |
| セキュリティ | 10 |
| パフォーマンス | 10 |

**フロントエンド（仮案）**
| 軸 | 配点 |
|---|---|
| 型安全性 | 20 |
| コード品質 | 20 |
| コード重複 | 15 |
| 依存方向 | 15 |
| Next.js/React BP | 10 |
| Lighthouse | 10 |
| バンドルサイズ | 10 |

---

## 🟡 優先度中（実装しながら調整可能）

### パフォーマンススコアの計算式
- `f(p95) = 20000 / p95_ms`（連続値・上限なし）にするか
- 段階スコア（例: p95 < 100ms → 10, < 200ms → 7, < 500ms → 3）にするか

### 足切りライン
- E2E以外にもスコアでの足切りを設けるか

### シードデータの規模
- 予約データ何件で性能差が出るか（100万? 1000万?）

---

## 🟢 優先度低

### ADRの必須テーマ
ADR-000（Architecture Overview）は必須。
その他に必須テーマを指定するか（例: 「DB設計」「並行制御方式」）

### スコア → 給与レンジ対応表
具体的なカットオフ値（実際の運用データが必要）

---

## ✅ 解決済み
- ~~問題テーマ~~ → 予約管理
- ~~バックエンドFW~~ → Hono（構造自由・型安全性高い）
- ~~フロントエンドFW~~ → Next.js
- ~~ORM~~ → 自由（採点対象外）
- ~~layers.json~~ → 廃止。ADR-000のモジュール宣言から依存違反を自動検出
- ~~カスタムAST解析~~ → ESLintプラグインで代替
- ~~Lighthouse~~ → 採用（配点小）
- ~~セキュリティツール~~ → Bearer
- ~~v1/v2~~ → 不要（ゼロから実装のため）
- ~~NestJS BP~~ → 不採用。FW固有BPは配点小。Node.js一般BPはコード品質に統合
- ~~アーキテクチャ自動採点~~ → ADR-000のDirectory Structureからモジュール境界を読み取り、dependency-cruiserで違反検出
